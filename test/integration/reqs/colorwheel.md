# ColorWheel Requirements

1. The base hue is always in valid range
2. There are always exactly 5 colors in the palette
3. Every color has valid saturation and lightness values
4. Contrast pair indices are valid (between 0 and 4)
5. When a mood is set (not Custom), all colors satisfy the mood constraints
6. Hues follow the selected harmony pattern
