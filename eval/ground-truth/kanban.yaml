project: kanban
module: KanbanDomain

# KanbanDomain has 79 flattened claims (8 Inv conjuncts, 2 helper predicates,
# many helper lemmas for sequence operations, Init/Step lemmas, and function preconditions).
#
# The pipeline correctly matches 6 invariant conjuncts to requirements.
# Most helper lemmas (RemoveFirst*, InsertAt*, FlattenCols*, etc.) are expected
# to appear as unexpected — they're proof infrastructure, not user requirements.
# The kanban compare API may omit the unexpected field when there are many helper claims.

proved:
  - requirementIndex: 1
    claimId: "pred:KanbanDomain.Inv:0"
  - requirementIndex: 3
    claimId: "pred:KanbanDomain.Inv:4"
  - requirementIndex: 4
    claimId: "pred:KanbanDomain.Inv:6"
  - requirementIndex: 6
    claimId: "pred:KanbanDomain.Inv:7"
  - requirementIndex: 2
    claimId: "pred:KanbanDomain.Inv:5"
  - requirementIndex: 7
    claimId: "pred:KanbanDomain.Inv:1"

missing:
  - requirementIndex: 5
    requirementText: "Adding a card to a full column"
  - requirementIndex: 8
    requirementText: "Moving a card preserves"

unexpected: []
