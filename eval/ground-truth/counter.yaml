project: counter
module: CounterDomain

# Flattened claims (4 total):
#   pred:CounterDomain.Inv:0              (m >= 0)
#   lemma:CounterDomain.InitSatisfiesInv:ensures:0  (Inv(Init()))
#   lemma:CounterDomain.StepPreservesInv:ensures:0  (Inv(Normalize(Apply(m, a))))
#   fn:CounterDomain.Apply:requires:0     (Inv(m))

proved:
  - requirementIndex: 1
    claimId: "pred:CounterDomain.Inv:0"
  - requirementIndex: 2
    claimId: "lemma:CounterDomain.InitSatisfiesInv:ensures:0"
  - requirementIndex: 3
    claimId: "lemma:CounterDomain.StepPreservesInv:ensures:0"

missing:
  - requirementIndex: 4
    requirementText: "Decrementing at zero"
  - requirementIndex: 5
    requirementText: "never exceeds 100"

unexpected:
  - claimId: "fn:CounterDomain.Apply:requires:0"
